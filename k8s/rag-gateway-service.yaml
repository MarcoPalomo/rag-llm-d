apiVersion: v1
kind: Service
metadata:
  name: rag-gateway
  namespace: llm-d
spec:
  type: NodePort
  selector:
    app: rag-gateway
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
